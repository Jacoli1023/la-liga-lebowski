# Dependencies — regenerable any time from package.json + pnpm-lock.yaml
node_modules/

# Build output (none yet; here for when we add a build step)
dist/

# Test coverage reports
coverage/

# The local PGlite database (spec 002, decision 4). Regenerable: pnpm sync:players.
# Derived data, never source. Tests use an in-memory PGlite and never touch this.
.data/
